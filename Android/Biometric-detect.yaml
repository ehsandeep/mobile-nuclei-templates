id: biometric-detect-test

info:
  name: Biometric or Fingerprint detect
  author: gaurang
  severity: info

file:
  - extensions:
      - all

    matchers:
      - type: word
        words:
          - "android.permission.USE_FINGERPRINT"
          - "android.permission.USE_BIOMETRIC"
